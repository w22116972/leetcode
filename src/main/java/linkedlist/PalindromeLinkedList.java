package linkedlist;

import datastructure.ListNode;

import java.util.HashMap;

public class PalindromeLinkedList {

    // final MiddleStack middleStack = new MiddleStack(app, env, defaultStackProps, backendStack.getEksConstruct());
    // s -> m -> e -> s
    public static boolean sol(ListNode head) {


        return true;
    }


}
